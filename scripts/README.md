# File Structure

| File | Relevance | Dependencies | Used by |
| --- | --- | --- | --- |
| acoustic_marker_model.py | Provides the command-line entrypoint for deterministic canonical marker SDF rendering. | models.acoustic_marker_model | operators, offline geometry checks |
| drive_to_thrusters.py | Runs the canonical deterministic four-regime actuator plant and applies signed forces plus synthetic PWM/RPM/current/thrust telemetry to Gazebo through installed pure models. | json, geometry_msgs, heron_msgs, rospy, std_msgs, models.four_regime_propulsion | heron_simulator/CMakeLists.txt, heron_simulator/config/thruster_dynamics.yaml, heron_simulator/launch/spawn_heron.launch |
| gazebo_with_xvfb.py | Runs gzserver on an owned headless display, with loopback TCP fallback for WSLg's non-sticky read-only X socket directory. | Python 3, Xvfb | heron_simulator/CMakeLists.txt, heron_simulator/launch/heron_world.launch |
| multibeam_raw.py | Converts only `dt100_link` Gazebo returns into exact-length Imagenex 83P v1.10 datagrams carrying the required provisional DT100 extrinsic revision and using the shared strict simulator parameter contract. | datetime, math, struct, rospy, ig_handle/SonarRawPacket, sensor_msgs/PointCloud2, models.parameters | heron_simulator/CMakeLists.txt, heron_simulator/launch/spawn_heron.launch, mariner/scripts/mapping/sonar/sonar_raw_to_cloud.py |
| ping360_profile_sim.py | Converts only `ping360_link` Gazebo returns into explicitly synthetic normalized planar profiles carrying and hashing the required provisional Ping360 extrinsic revision. | hashlib, struct, rospy, ig_handle/SonarProfile, models | heron_simulator/CMakeLists.txt, heron_simulator/launch/spawn_heron.launch |
| profile_env_run.sh | Runs the profile env run shell workflow. | None | heron_simulator/CMakeLists.txt, heron_simulator/launch/spawn_heron.launch |
| scenarios.py | Resolves one coherent simulator world, entity, spawn, marker, and offset bundle through active ROS package roots, enumerates configured scenarios, and strictly validates scenario booleans. | pathlib, typing, rospkg, yaml, models.parameters | heron_simulator/CMakeLists.txt, grande/run.py |
| sim_ig_timing.py | Publishes IG Handle-style timing topics from simulated sensor timestamps with strict timestamp-deduplication configuration. | typing, rospy, sensor_msgs, models.parameters | heron_simulator/CMakeLists.txt, heron_simulator/launch/spawn_heron.launch |
| sim_sense.py | Publishes explicitly synthetic Heron `/sense` and 1 Hz `/status` contracts whose battery-side current, voltage, uptime, and accumulated motor-energy fields agree with the canonical simulator plant state. | json, time, rospy, heron_msgs, std_msgs | heron_simulator/CMakeLists.txt, heron_simulator/launch/spawn_heron.launch |
| spawn_acoustic_marker.py | Resolves the canonical RANGE_AID descriptor and instance, strictly validates provisional authorization, revision consistency, and boolean configuration, and spawns the generated static model in Gazebo. | rospy, gazebo_msgs, geometry_msgs, yaml, models | heron_simulator/launch/spawn_acoustic_marker.launch |
