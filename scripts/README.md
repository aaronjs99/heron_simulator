# File Structure

| File | Relevance | Dependencies | Used by |
| --- | --- | --- | --- |
| acoustic_marker_model.py | Provides the command-line entrypoint for deterministic canonical marker SDF rendering. | heron_simulator_runtime.acoustic_marker_model | operators, offline geometry checks |
| drive_to_thrusters.py | Runs the internally consistent provisional four-regime actuator plant, strictly admits its model-selection flag, and applies signed forces plus synthetic PWM/RPM/current/thrust telemetry to Gazebo through installed pure models. | json, pathlib, geometry_msgs, heron_simulator_runtime | heron_simulator/CMakeLists.txt, heron_simulator/config/thruster_dynamics.yaml, heron_simulator/launch/spawn_heron.launch |
| gazebo_with_xvfb.py | Runs gzserver on an owned headless display, with loopback TCP fallback for WSLg's non-sticky read-only X socket directory. | Python 3, Xvfb | heron_simulator/CMakeLists.txt, heron_simulator/launch/heron_world.launch |
| multibeam_raw.py | Converts only `dt100_link` Gazebo returns into exact-length Imagenex 83P v1.10 datagrams carrying the required provisional DT100 extrinsic revision and using the shared strict simulator parameter contract. | datetime, math, struct, rospy, ig_handle/SonarRawPacket, sensor_msgs/PointCloud2, heron_simulator_runtime.parameters | heron_simulator/CMakeLists.txt, heron_simulator/launch/spawn_heron.launch, mariner/scripts/mapping/sonar/sonar_raw_to_cloud.py |
| ping360_profile_sim.py | Converts only `ping360_link` Gazebo returns into explicitly synthetic normalized planar profiles carrying and hashing the required provisional Ping360 extrinsic revision. | hashlib, struct, rospy, ig_handle/SonarProfile, heron_simulator_runtime | heron_simulator/CMakeLists.txt, heron_simulator/launch/spawn_heron.launch |
| profile_env_run.sh | Runs the profile env run shell workflow. | None | heron_simulator/CMakeLists.txt, heron_simulator/launch/spawn_heron.launch |
| scenarios.py | Resolves simulator world, entity, spawn, marker, and offset values through active ROS package roots, strictly validates scenario booleans, and leaves mission and navigation policy to GRANDE profiles. | pathlib, typing, rospkg, yaml, heron_simulator_runtime.parameters | heron_simulator/CMakeLists.txt, grande/launch/bringup.launch |
| sim_ig_timing.py | Publishes IG Handle-style timing topics from simulated sensor timestamps with strict timestamp-deduplication configuration. | typing, rospy, sensor_msgs, heron_simulator_runtime.parameters | heron_simulator/CMakeLists.txt, heron_simulator/launch/spawn_heron.launch |
| sim_sense.py | Publishes explicitly synthetic Heron `/sense` and 1 Hz `/status` contracts whose battery-side current, voltage, uptime, and accumulated motor-energy fields agree with the provisional actuator state. | json, time, rospy, heron_msgs, std_msgs | heron_simulator/CMakeLists.txt, heron_simulator/launch/spawn_heron.launch |
| spawn_acoustic_marker.py | Resolves the canonical RANGE_AID descriptor and instance, strictly validates provisional authorization, revision consistency, and boolean configuration, and spawns the generated static model in Gazebo. | rospy, gazebo_msgs, geometry_msgs, yaml, heron_simulator_runtime | heron_simulator/launch/spawn_acoustic_marker.launch |
