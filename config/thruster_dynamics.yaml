# Benchmark simulation thruster profile used by cmd_drive_translate.py.
# Keep this file limited to parameters the active translator reads.
rate: 30.0
cmd_timeout: 0.35
max_fwd_thrust: 45.0
max_bck_thrust: 25.0
left_scale: 1.0
right_scale: 1.0
