# File Structure

| File | Relevance | Dependencies | Used by |
| --- | --- | --- | --- |
| index.yaml | Maps named simulation scenarios to their owned bundles. | Scenario YAML files | scenario.catalog |

The evidence acquisition bundles define four policy fixtures. The navigation bundle defines the nominal sensor-observable prerequisite room.
