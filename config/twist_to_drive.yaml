# Benchmark simulation mixer profile.
# Keep the normalization aligned with the navigation stack rather than the hull's
# theoretical top speed so forward commands do not collapse into near-pivot turns.
max_linear: 1.35
max_angular: 0.30
mix_gain: 0.95
wheelbase: 0.755
deadband: 0.01
floor: 0.05
alpha: 0.40
rate: 30.0
timeout: 0.35
ramp_down: 0.25
sign_hysteresis: 0.02
sign_dwell: 0.05
no_reverse_v_thresh: 0.20
low_speed_turn_mix: 0.40
low_speed_turn_v_thresh: 0.25
